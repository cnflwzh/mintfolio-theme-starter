# 参与开发

欢迎提交能复现的问题、文档修正和小范围改进。较大的接口或布局改动，先开 issue 说明想解决什么问题，方便讨论范围。

## 本地准备

需要 Node.js >= 22.12.0。克隆后运行 npm ci，然后运行 npm run check。vendor 包是尚未公开发布的依赖快照，保留这些文件就能独立安装，无需相邻源码目录。

## 提交改动

从 main 新建分支，通过 pull request 合并。一个 PR 尽量处理一件事，描述问题、改动后的行为和实际做过的验证。提交标题使用 feat:、fix:、docs: 或 refactor: 前缀。

只为核心行为增加单元测试，优先覆盖真实需求和回归风险。相关改动完成后集中检查；纯文档修订不需要重复运行整套测试。界面改动请附实际截图，说明检查过的尺寸。

主题通过公开 API 获取内容，不读取站点原始配置或其他仓库的私有源码。更改公开接口时同步类型、示例和迁移说明。

## 评审与许可

主分支要求通过 CI 并解决评审讨论。目前主要由一名维护者管理，不强制第二人批准；欢迎其他贡献者参与评审。不要强推 main，或为了通过检查临时跳过失败项。

代码按 GPL-3.0-only 发布。引入第三方代码、字体或素材时，一并保留来源和对应许可证。
